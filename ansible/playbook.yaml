- name: confiure
  hosts: private
  remote_user: ubuntu
  become: true
  tasks:
    - name: update cache
      apt: update_cache=yes

    - name: instal open jdk
      apt:
        name: default-jdk-headless
        state: present 
    
    - name: install docker
      apt:
        name: docker.io
        state: present

    - name: install wget
      apt:
        name: wget
        state: present

    - name: adding user to group docker
      user:
        name: "ubuntu"
        groups: docker
        append: yes

      
